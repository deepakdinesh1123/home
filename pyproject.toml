[project]
name = "home"
version = "0.0.1"
description = "Home"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.7",
    "mkdocs-material>=9.5.49",
    "mkdocs-minify-plugin>=0.8.0",
]

[tool.uv]
required-version = "0.4.30"